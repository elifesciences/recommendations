version: '3.4'

services:
    ci:
        build:
            context: .
            dockerfile: Dockerfile
            target: ci
            args:
                php_version: ${PHP_VERSION}
        environment:
            - APP_ENV=ci
